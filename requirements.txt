# Núcleo
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
PyYAML>=6.0
joblib>=1.3

# Feed de dados — o pacote oficial só existe para Windows (fala com o
# terminal MT5 instalado na máquina do trader).
MetaTrader5>=5.0.45; sys_platform == "win32"

# Desenvolvimento / testes
pytest>=7.0
